*bird2.txt*	BIRD Configuration Syntax for Vim

Author:  BIRD Chinese Community <dev-bird@xmsl.dev>
License: MPL-2.0
Version: 1.0.13

==============================================================================
CONTENTS					*bird2-contents*

    1. Introduction ................ |bird2-introduction|
    2. Installation ................ |bird2-installation|
    3. Updating ..................... |bird2-updating|
    4. Filetype Detection ......... |bird2-filetype-detection|
    5. Configuration ............... |bird2-configuration|
    6. Syntax Highlighting ........ |bird2-syntax|
    7. Commands .................... |bird2-commands|
    8. Mappings .................... |bird2-mappings|
    9. Related Projects ............ |bird2-related|
   10. Changelog ................... |bird2-changelog|

==============================================================================
INTRODUCTION					*bird2-introduction*

The BIRD.vim plugin provides syntax highlighting and filetype detection for
BIRD 2 and BIRD 3 (BIRD Internet Routing Daemon) configuration files.

BIRD is a dynamic IP routing daemon supporting IPv4 and IPv6 protocols,
including BGP, OSPF, RIP, and static routing.

Features:
	- Syntax highlighting aligned with BIRD 2.19 and BIRD 3.3
	- Automatic filetype detection
	- Heuristic detection for generic .conf files
	- Filetype plugin with sensible defaults

==============================================================================
INSTALLATION					*bird2-installation*

VIM-PLUG ~

	Add to your .vimrc: >
		Plug 'bird-chinese-community/BIRD.vim'
<

VUNDLE ~

	Add to your .vimrc: >
		Plugin 'bird-chinese-community/BIRD.vim'
<

PACK.NVIM (Vim 8+ / Neovim) ~

	Clone to your pack directory: >
		git clone https://github.com/bird-chinese-community/BIRD.vim \
		  ~/.vim/pack/plugins/start/BIRD.vim
<

MANUAL ~

	Run the install script: >
		git clone https://github.com/bird-chinese-community/BIRD.vim
		cd BIRD.vim
		bash scripts/install.sh
<

After installation, generate help tags: >
	:helptags ~/.vim/doc
<

==============================================================================
UPDATING					*bird2-updating*

BIRD.vim was renamed from BIRD2.vim. GitHub redirects the old repository URL,
and the bird2 filetype, runtime filenames, mappings, and variables remain
compatible. Update the repository name in your plugin-manager configuration.

vim-plug: >
	:PlugUpdate BIRD.vim
<

Vundle: >
	:PluginUpdate
<

Native package: >
	git -C ~/.vim/pack/plugins/start/BIRD.vim pull --ff-only
<

For an existing checkout using the old directory name: >
	git -C /path/to/bird2.vim remote set-url origin \
	  https://github.com/bird-chinese-community/BIRD.vim.git
	git -C /path/to/bird2.vim pull --ff-only
	bash /path/to/bird2.vim/scripts/install.sh
<

==============================================================================
FILETYPE DETECTION				*bird2-filetype-detection*

The plugin automatically detects BIRD 2 and BIRD 3 configuration files by:

EXTENSIONS ~
	*.bird, *.bird2, *.bird3, *.bird.conf, *.bird2.conf, *.bird3.conf

FILENAMES ~
	bird.conf, bird2.conf, bird3.conf, bird6.conf, bird-*.conf

CONTENT HEURISTICS ~
	For generic .conf files, the plugin scans the first 200 lines. BIRD-only
	protocols, typed tables, and router id are strong signals. Otherwise two
	independent policy/declaration signals are required to avoid false positives.
	Comments are ignored, and an existing non-conf filetype is preserved.

To verify filetype detection: >
	:verbose set ft?
<

==============================================================================
CONFIGURATION					*bird2-configuration*

Disable heuristic detection: >
	let g:bird2_heuristic_detect = 0
<

Add custom file extensions: >
	autocmd BufRead,BufNewFile *.myext setfiletype bird2
<

Change comment string (default is #): >
	autocmd FileType bird2 setlocal commentstring=#\ %s
<

==============================================================================
SYNTAX HIGHLIGHTING				*bird2-syntax*

The syntax file highlights:

KEYWORDS ~
	protocol, router, id, template, filter, function, table, define

PROTOCOLS ~
	bgp, ospf, rip, device, direct, kernel, pipe, babel, radv, rpki, bfd,
	bmp, static, l3vpn, mrt, perf, aggregator, bridge, evpn

DATA TYPES ~
	- Integers: 42, 0x1a
	- Strings: "text", 'text'
	- IP addresses: 192.168.1.1, 2001:db8::1
	- Prefixes: 10.0.0.0/8, 2001:db8::/32
	- AS numbers: 65000
	- VPN route distinguishers: 65000:100

COMMENTS ~
	# Single-line comments
	/* Multi-line comments */

==============================================================================
COMMANDS						*bird2-commands*

No custom commands are provided by this plugin. Use standard Vim commands
for file manipulation and configuration.

==============================================================================
MAPPINGS					*bird2-mappings*

<Plug>Bird2Comment		Toggle comment (visual and normal mode)

To map to a custom key: >
	xmap <leader>c <Plug>Bird2Comment
	nmap <leader>c <Plug>Bird2Comment
<

==============================================================================
RELATED PROJECTS					*bird2-related*

BIRD-tm-language-grammar ~
	TextMate grammar for BIRD2
	https://github.com/bird-chinese-community/bird-tm-language-grammar

BIRD.nvim ~
	Neovim plugin with Lua support
	https://github.com/bird-chinese-community/BIRD.nvim

vscode-bird2 ~
	VS Code extension
	https://github.com/bird-chinese-community/vscode-bird2-conf

==============================================================================
CHANGELOG					*bird2-changelog*

1.0.13 (2026-07-17)~
	- Align keywords, enums, CLI phrases, attributes, and operators with current
	  BIRD 2.19 and BIRD 3.3 source trees
	- Add scored content detection with bounded comment-aware scanning
	- Implement working buffer-local comment mappings without changing cwd

1.0.9 (2026-03-06)~
	- Add local/neighbor statement highlighting for symbolic ASNs
	- Highlight neighbor scoped-interface, port, and symbolic target variants
	- Align neighbor template coverage with BIRD-LSP parser updates

1.0.7 (2026-02-28)~
	- Add missing RPKI keyword highlighting (transport, version, key phrases)
	- Add vpn4/vpn6/roa4 address keyword highlighting

1.0.6 (2025-08-08)~
	- Initial release
	- Syntax highlighting for BIRD2 2.17+
	- Filetype detection with heuristics
	- Filetype plugin support

==============================================================================
 vim:tw=78:ts=8:ft=help:norl:
